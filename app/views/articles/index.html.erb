<% if logged_in? %>
    <div class="page-header">
      <h2>すべての投稿</h2>
    </div>
    <%= render 'article_list', articles: @articles, ranking_good_counts: @ranking_good_counts %>

    <%= paginate @articles %>

<% end %>